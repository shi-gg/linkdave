name: Test server

on:
    pull_request:
        branches:
            - main

permissions:
    contents: read

jobs:
    test:
        name: Build and Push to GHCR
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v5
            - name: Build Docker image
              run: docker build .
