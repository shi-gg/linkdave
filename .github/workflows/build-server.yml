name: server

on:
    push:
        branches:
            - main

permissions:
    contents: read
    packages: write

jobs:
    build:
        name: Publish GHCR
        runs-on: ubuntu-latest
        env:
            IMAGE_BASE: ghcr.io/${{ github.repository_owner }}/${{ github.event.repository.name }}

        steps:
            - uses: actions/checkout@v6
            - name: Log in to GitHub Container Registry
              uses: docker/login-action@v3
              with:
                  registry: ghcr.io
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - uses: oven-sh/setup-bun@v2
              with:
                  bun-version: "latest"

            - name: Check if package version changed
              id: check_publish
              run: bun ./scripts/check-version.ts

            - name: Build Docker image
              run: docker build --build-arg BUILD_VERSION=${{ env.version }} -t ${{ env.IMAGE_BASE }}:${{ github.sha }} -t ${{ env.IMAGE_BASE }}:latest -t ${{ env.IMAGE_BASE }}:${{ env.version }} .

            - name: Push Docker image
              run: docker push ${{ env.IMAGE_BASE }}:${{ github.sha }}

            - name: Push Docker image (Release)
              if: env.publish == 'true'
              run: |
                  docker push ${{ env.IMAGE_BASE }}:latest
                  docker push ${{ env.IMAGE_BASE }}:${{ env.version }}
